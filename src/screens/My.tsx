import { StyleSheet, Text, View } from 'react-native';
import React from 'react';
import { MyProps } from '~/navigators/stackNav/MyStackNav';

const My = ({}: MyProps) => {
  return (
    <View>
      <Text>My</Text>
    </View>
  );
};

export default My;

const styles = StyleSheet.create({});
